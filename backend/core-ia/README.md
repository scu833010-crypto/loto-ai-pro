# LOTO IA RD — CORE IA

Paquete independiente para analisis descriptivo de resultados historicos reales. No contiene frontend, Expo, backend principal ni datos de loteria.

## Ejecucion

```bash
npm test
```

Consulta `docs/ARCHITECTURE.md`, `docs/INTEGRATION.md` y `docs/DEPENDENCIES.md`.

Tambien estan disponibles `docs/DEPENDENCY_DIAGRAM.md` y `docs/MODULES.md`.
